{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [
      1,
      "always",
      ["feat", "fix", "docs", "style", "refactor", "test", "ci", "chore"]
    ],
    "scope-enum": [
      2,
      "always",
      ["common", "config", "core"]
    ]
  }
}
